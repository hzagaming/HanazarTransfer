# Hanazar Transfer v1.1.0

发布日期：2026-08-16

本次版本继续加强纯前端 P2P 的连接状态一致性，避免通道刚建立又瞬间关闭时页面错误停留在“已直连”。

## 本次更新

- 修复 DataChannel 打开后发送设备身份失败时产生未捕获异常的问题
- 身份消息发送失败后立即关闭无效连接、隐藏传输工作区并返回重新配对状态
- 将控制消息发送保护复用于文字和连接问候，避免相同竞态出现分叉行为
- 保留失败前的文字输入，不创建错误的“已发送”记录
- 回归验证双向文字、双方同时发送普通与零字节文件、取消、送达确认和下载内容完整性
- 复核局域网自动发现、文字弹窗、多文件中转、移动端布局、键盘焦点和错误反馈

提示音仍默认关闭，只有用户主动开启后才创建音频上下文，关闭后立即挂起。本版本继续不加入循环 BGM。

## 使用提醒

打开 [GitHub Pages](https://hzagaming.github.io/HanazarTransfer/) 后进入“P2P 直传”即可使用。纯前端模式仍需交换一次邀请和回复；项目不提供 TURN 中继，同一 Wi-Fi 成功率最高。需要自动发现设备时，可在局域网电脑运行 `npm start`。

[查看历史公告](docs/announcements/README.md)
