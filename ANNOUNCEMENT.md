# Hanazar Transfer v1.0.0

发布日期：2026-08-15

Hanazar Transfer 进入首个稳定版本。GitHub Pages 纯前端 P2P 直传与局域网自动发现模式均已完成完整传输、异常恢复、移动端和可访问性复核。

## 本次更新

- 将现有双模式传输能力确认为稳定接口并发布 v1.0.0
- GitHub Pages 可直接使用 WebRTC 加密直传文件与文字，不需要 Hanazar Transfer 后端
- 局域网服务支持设备自动发现、文字通知、传输码、多文件流式中转和到期清理
- 修复 P2P 文字发送瞬间连接关闭时产生未捕获异常且缺少错误提示的问题
- 无效 SSE 设备凭证现在会在事件流建立前返回结构化 403，不再表现为异常断流
- GitHub Pages 首页加入 `no-referrer`，避免误入链接中的查询参数进入导航来源信息
- 复核取消、送达确认、旧通道消息隔离、零字节文件、双方同时发送和断线后下载记录
- 复核 320px 至 1440px 布局、键盘焦点、读屏语义、缩减动画、提示音和错误反馈

提示音仍默认关闭，只有用户主动开启后才创建音频上下文，关闭后立即挂起。稳定版继续不加入循环 BGM，避免干扰短时传输任务。

## 使用提醒

打开 [GitHub Pages](https://hzagaming.github.io/HanazarTransfer/) 后进入“P2P 直传”即可使用。纯前端模式仍需交换一次邀请和回复；项目不提供 TURN 中继，同一 Wi-Fi 成功率最高。需要自动发现设备时，可在局域网电脑运行 `npm start`。

[查看历史公告](docs/announcements/README.md)
