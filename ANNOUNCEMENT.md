# Hanazar Transfer v0.3.1

发布日期：2026-08-11

本次版本集中修复 v0.3.0 上线后在取消、并发操作、送达反馈和键盘焦点方面的可靠性问题，同时复核局域网与纯前端 P2P 两种模式。

## 本次更新

- P2P 取消现在可立即终止等待接收确认或发送缓冲，不再因对端延迟而长时间停在“正在取消”
- 文件全部发出后进入明确的“正在确认送达”阶段，防止已送达文件被误操作为取消
- 旧 DataChannel 的延迟消息不会再干扰新连接或触发错误断线
- 局域网上传期间彻底锁定文件队列，拖放不会再把新文件加入已经创建的传输
- 附近设备通知失败后，结果页不再错误显示“已发送给该设备”，会明确引导使用传输码
- 连接完成、发送结果、接收结果、清空记录与重新开始后恢复合理键盘焦点
- 统一上传期间移除与清空按钮的禁用视觉，减少可点击状态误判
- 重新验证双向文字、双方同时发送文件、零字节文件、中途取消、断线历史与下载内容完整性
- 复核 320px 至 1440px 布局、缩减动画、读屏语义、点击区域、异常提示和复制降级路径

提示音仍默认关闭，只有用户主动开启后才创建音频上下文，并会在关闭后立即挂起。本版本继续不加入循环 BGM，避免干扰短时传输任务和触发浏览器自动播放限制。

## 使用提醒

打开 [GitHub Pages](https://hzagaming.github.io/HanazarTransfer/) 后进入“P2P 直传”即可使用。纯前端模式仍需交换一次邀请和回复；没有 TURN 中继时，同一 Wi-Fi 成功率最高。需要自动发现设备时，可在局域网电脑运行 `npm start`。

[查看历史公告](docs/announcements/README.md)
