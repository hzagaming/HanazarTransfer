# Hanazar Transfer v0.2.2

发布日期：2026-08-09

本次版本重点完善 GitHub Pages 展示入口与移动端可靠性，让静态展示和局域网传输两种运行模式不再互相混淆。

## 本次更新

- GitHub Pages 新增明确的展示模式说明，并提供局域网启动入口
- Pages 不再反复请求不存在的 API，也不会误显示“服务在线”
- 展示模式禁用文件发送与传输码查询，避免用户进入必然失败的流程
- 改用 `pagehide` 清理移动端离开页面时的未完成传输
- 修复剪贴板降级复制失败后仍提示成功的问题
- 将浏览器网络错误统一为清晰的中文反馈
- 错误 Toast 改为及时播报，并允许相同消息再次被读屏器识别
- 统一文本框字体，继续覆盖 320px 至桌面宽屏布局

继续不加入循环背景音乐：文件传输属于短时工具场景，持续 BGM 会干扰使用，也受浏览器自动播放策略限制。轻量提示音仍默认关闭，仅在用户主动开启后播放。

## 使用提醒

GitHub Pages 只能展示静态页面，无法承担设备发现、事件流和文件中转。请在局域网内的一台电脑运行 `npm start`，再让其他设备打开终端显示的局域网 IP 地址。

[查看历史公告](docs/announcements/README.md)
